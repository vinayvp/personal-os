
import React from 'react';
import HabitTracker from './HabitTracker';

// This component now serves as a simple wrapper for the new HabitTracker
const TrackingApp = () => {
  return <HabitTracker />;
};

export default TrackingApp;
